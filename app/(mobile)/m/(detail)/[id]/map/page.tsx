'use client';

const MobileMap = ({ params: { id } }: { params: { id: string } }) => {
    console.log(id);
    return (
        <div>
            <div>map</div>
        </div>
    );
};

export default MobileMap;

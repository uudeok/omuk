const palette = {
    mainColor: '#f8c471',
    mainColorLg: '#000000',
};

const devise = {};

const fonts = {};

const theme = {
    palette,
    devise,
    fonts,
};

export type ThemeType = typeof theme;

export default theme;
